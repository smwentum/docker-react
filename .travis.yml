language: generic 
sudo: required
services: 
  - docker

before_install:
  - docker build -t smwentum/docker-react -f Dockerfile.dev .

script:
  - docker run -e CI=true smwentum/docker-react npm run test -- --coverage